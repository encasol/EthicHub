pragma solidity >=0.4.25 <0.6.0;

contract proxy {
    //Para poder mover el contrato de una direccion a otra facilemnte
    address public poolAddress;

    constructor (address _address) public {
        poolAddress = address;

    }
}

contract compensationPool {




    function bugdetected {

    //Esta funcion sirve para bloquear un contrato con un bug y tras haber creado otro smart contract sin el bug enviarle el dinero



    }

}